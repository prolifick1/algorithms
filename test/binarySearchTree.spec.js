const { expect } = require('chai');
const BinarySearchTree = '../src/binarySearchTree';

describe('Binary Search Tree Implementation', () => {
  let BST;

  beforeEach('Create new BST', () => {
    BST = new BinarySearchTree();
  });

  it('Insert method adds new nodes', () => {});
});
